import pymysql
from settings import *

# 数据库数据校验
def verify_mysql(data, sql, params):
    # db = pymysql.connect(
    #     host=host,
    #     user=user,
    #     password=password,
    #     database=database,
    # )
    #
    # cursor = db.cursor()
    #
    # cursor.execute(sql)
    #
    # res = cursor.fetchone()

    return True
